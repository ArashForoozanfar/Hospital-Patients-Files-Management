puts("Please enter which number of patients do you want to add:");
scanf("%d", &x);
printf("So you want to add %d patients information\n", x);
for (int i = 0; i < x; i++) {
  printf("FirstName:");
  scanf("%s", p[i].fName);
  fprintf(F, "FirstName: %s\n", p[i].fName);
  fprintf(D, "FirstName: %s\n", p[i].fName);
  printf("LastName:");
  scanf("%s", p[i].lName);
  fprintf(F, "LastName: %s\n", p[i].lName);
  fprintf(D, "LastName: %s\n", p[i].lName);
  printf("ID:");
  scanf("%lld", &p[i].id);
  fprintf(F, "ID: %lld\n", p[i].id);
  printf("PhoneNumber:");
  scanf("%lld", &p[i].pNumber);
  fprintf(F, "PhoneNumber: %lld\n", p[i].pNumber);
  srand(time(0));
  p[i].fId = rand();
  printf("FileNumber is %lld\n", p[i].fId);
  fprintf(F, "FileNumber: %lld\n", p[i].fId);
  printf("Reason for Reference:");
  scanf("%s", p[i].cause);
  fprintf(F, "Reason for Reference: %s\n", p[i].cause);
  printf("Price:");
  scanf("%lld", &p[i].price);
  fprintf(F, "Price: %lld\n", p[i].price);
  printf("Price Date:");
  cur_time = time(NULL);
  *p[i].price_Date1 = ctime(&cur_time);
  printf("Price Date is %s", *p[i].price_Date1);
  fprintf(F, "Price Date: %s", *p[i].price_Date1);
  printf("Prescription Date:");
  cur_time = time(NULL);
  *p[i].noDate1 = ctime(&cur_time);
  printf("Prescription Date is %s", *p[i].noDate1);
  fprintf(F, "Prescription Date: %s", *p[i].noDate1);
  fprintf(D, "Prescription Date: %s", *p[i].noDate1);
  printf("Drugs:");
  scanf("%s", p[i].Drug);
  fprintf(F, "Drugs: %s\n", p[i].Drug);
  fprintf(D, "Drugs: %s\n", p[i].Drug);
  printf("Drug Dosage:");
  scanf("%s", p[i].dosage);
  fprintf(F, "Drug Dosage: %s\n", p[i].dosage);
  fprintf(D, "Drug Dosage: %s\n", p[i].dosage);
  fprintf(F, "\n");
  fprintf(D, "\n");
}
d += x;
